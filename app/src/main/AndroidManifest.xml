<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.minsung.examples">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tutorial.tutorial" />
        <activity android:name=".Tutorial.tutorial2" />
        <activity android:name=".Info.InfoMain" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Info.Register"></activity>
        <activity android:name=".Info.RegisterOption"></activity>
        <activity android:name=".CardAuthenticationActivity"></activity>
        <activity android:name=".Info.RegisterNumber"></activity>
    </application>

</manifest>